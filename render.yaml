services:
  - type: web
    name: tactical-walkie
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3000
